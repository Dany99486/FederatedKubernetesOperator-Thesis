apiVersion: optimizer.uc.pt/v1
kind: FederatedPlacement
metadata:
  labels:
    app.kubernetes.io/name: federated-operator
    app.kubernetes.io/managed-by: kustomize
  name: federatedplacement-sample
spec:
  # TODO(user): Add fields here
