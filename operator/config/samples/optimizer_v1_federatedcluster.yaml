apiVersion: optimizer.uc.pt/v1
kind: FederatedCluster
metadata:
  labels:
    app.kubernetes.io/name: federated-operator
    app.kubernetes.io/managed-by: kustomize
  name: federatedcluster-sample
spec:
  # zone: Geographic identifier matching the latencyZones in FederatedPlacement
  zone: "aveiro"
  
  # isOnPremise: If true, physical capacity constraints (Capacity_j) are strictly enforced
  isOnPremise: false
