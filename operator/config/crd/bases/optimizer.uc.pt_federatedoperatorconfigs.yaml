---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: federatedoperatorconfigs.optimizer.uc.pt
spec:
  group: optimizer.uc.pt
  names:
    kind: FederatedOperatorConfig
    listKind: FederatedOperatorConfigList
    plural: federatedoperatorconfigs
    singular: federatedoperatorconfig
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: FederatedOperatorConfig is the Schema for the federatedoperatorconfigs
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: FederatedOperatorConfigSpec defines the global policy parameters.
            properties:
              normalizationConstant:
                description: |-
                  NormalizationConstant ($k$): Scale used to normalize latency
                  values against monetary values
                type: string
              optimizationWeight:
                description: |-
                  OptimizationWeight ($\alpha$): Balance between infrastructure cost
                  and latency penalty between 0 and 1, where 0 means only latency is considered and 1 means only cost is considered
                type: string
              totalBudget:
                description: |-
                  TotalBudget ($B$): Maximum financial limit for the operation cost
                  of all federated workloads
                type: string
            required:
            - normalizationConstant
            - optimizationWeight
            - totalBudget
            type: object
          status:
            description: FederatedOperatorConfigStatus defines the observed global
              metrics.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the config state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              globalMisalignmentScore:
                description: |-
                  GlobalMisalignmentScore ($\mathcal{L}(x)$): Global cluster latency score
                  based on geographical targets
                type: string
              totalCurrentCost:
                description: |-
                  TotalCurrentCost ($\mathcal{C}(x)$): Current total cluster cost detected
                  via Prometheus and OpenCost in the CMC
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
